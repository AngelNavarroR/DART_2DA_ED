main(){
  var velocidad = 20;
  var pi = 3.14;
  var resultado = (velocidad * pi);
  print("    " + resultado.toString());
  // Ejemplo de for loop
  // Const se asigan su espacio  en el momento de compilacion
  const collection = {1,2,3,4,5,6};
  for (var element in collection) {
     print("    " + (element * pi).toString());
  }
  // Final reserva cuando se ejecuta

  // late
  late String fecha;

}